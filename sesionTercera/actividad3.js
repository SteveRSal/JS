let number = parseFloat(prompt('Ingrese un numero'));
let operation = number / 2;

if (number >=2) {
    alert('El ' + number + ' es divisible entre 2. \nEl resultado de la division es:  ' + operation)
} else {
    alert('El ' + number + ' no es divisible entre 2.')
}